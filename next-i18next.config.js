const path = require('path');

module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'es'],
    domains: [
      {
        domain: 'garmanazareno.vercel.app',
        defaultLocale: 'en'
      }
    ]
  }
};
