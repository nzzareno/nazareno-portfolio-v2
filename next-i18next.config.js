const path = require('path');

module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'es'],
    domains: [
      {
        domain: 'https://garmanazareno.vercel.app',
        defaultLocale: 'en'
      }
    ]
  }
};
